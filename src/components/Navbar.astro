---
import { dataNavbar } from "../constants/const"
---

<nav
  class="flex justify-center fixed w-full lg:justify-between py-6 px-10 z-10 transition-[top] duration-300 bg-transparent backdrop-blur-md backdrop-saturate-150"
>
  <div class="hidden lg:flex">
    <a href="/">Harry Gómez</a>
  </div>
  <ul class="flex gap-8">
    {dataNavbar.map(({ name, path }) =>
      <li>{name}</li>
    )}
  </ul>
</nav>
